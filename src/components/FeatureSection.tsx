import { Card, CardContent } from "@/components/ui/card";

export const FeatureSection = () => {
  return (
    <section className="py-24 px-4 bg-gray-50">
      <div className="max-w-7xl mx-auto">
        {/* Content removed since words are now in Hero section */}
      </div>
    </section>
  );
};
